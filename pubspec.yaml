name: flutter_charts_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  fl_chart: ^0.35.0
  charts_flutter: ^0.10.0
  responsive_framework: ^0.1.0
  mobx: ^1.2.1+4
  flutter_mobx: ^1.1.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.11.1
  mobx_codegen: ^1.1.2

flutter:

  uses-material-design: true

  assets:
    - assets/